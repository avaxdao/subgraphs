{
  "name": "power-tracker",
  "license": "UNLICENSED",
  "repository": "git@github.com:avaxdao/subgraphs.git",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ avaxdao/power-tracker",
    "create-local": "graph create --node http://localhost:8020/ avaxdao/power-tracker",
    "remove-local": "graph remove --node http://localhost:8020/ avaxdao/power-tracker",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 avaxdao/power-tracker"
  }
}
